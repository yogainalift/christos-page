import React from 'react'
import {Container} from 'Common'
import {SkillsWrapper, Wrapper} from './styles'

export const Skills = () => (
  <Wrapper id="about">
    <SkillsWrapper as={Container}>
			HTML, SCSS, Angular2+, React, VueJs, redux, graphql
    </SkillsWrapper>
  </Wrapper>
)
